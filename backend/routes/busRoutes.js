const express = require('express');
const router = express.Router();

// Placeholder - we'll implement later
router.get('/', (req, res) => {
  res.json({ 
    success: true,
    message: 'Bus routes - coming soon' 
  });
});

module.exports = router;